# Instalação: pip install -r requirements.txt
# Dev: pip install -r requirements.txt -e ".[dev]"  (com pyproject) ou pip install pytest httpx

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic-settings>=2.6.0
